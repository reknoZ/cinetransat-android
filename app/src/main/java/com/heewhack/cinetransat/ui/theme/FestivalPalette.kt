package com.heewhack.cinetransat.ui.theme

import androidx.compose.ui.graphics.Color

/** 2026 brand palette — navy `RGB(25, 43, 89)` and pink `RGB(200, 109, 144)`. */

/** Programme / splash background — `#192B59`. */
val FestivalProgramBackground = Color(0xFF192B59)

/** Primary text on programme screens. */
val FestivalProgramTitle = Color(0xFFFFF8FA)

/** Passed / de-emphasized programme text — also used as poster border (secondary). */
val FestivalProgramTitleMuted = Color(0xFFB0B8CC)

/** Week pager track, segmented controls. */
val FestivalProgramPagerTrack = Color(0xFF263A6C)

/** Brand accent — headings, tabs, selection. */
val FestivalAccent = Color(0xFFC86D90)

/** Accent in dark contexts (slightly brighter pink). */
val FestivalAccentBright = Color(0xFFDC82A5)
